/** 默认用户行为队列自动上报的最大长度 */
export const DEFAULT_MAX_LENGTH_TO_REPORT = 10

/** 默认是否记录 XHR 请求行为 */
export const DEFAULT_RECORD_XHR = true

/** 默认是否记录 fetch 请求行为 */
export const DEFAULT_RECORD_FETCH = true
