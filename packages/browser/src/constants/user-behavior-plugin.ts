/** 默认用户行为队列自动上报的最大长度 */
export const DEFAULT_MAX_LENGTH_TO_REPORT = 3

/** 默认是否记录 XHR 请求行为 */
export const DEFAULT_RECORD_XHR = true

/** 默认是否记录 fetch 请求行为 */
export const DEFAULT_RECORD_FETCH = true

/** 默认的每隔多长时间上报一次用户行为事件 */
export const DEFAULT_REPORT_INTERVAL = 3000
